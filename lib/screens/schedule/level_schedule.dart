import 'package:flutter/material.dart';

class LevelSchedule extends StatelessWidget {
  int level;

  LevelSchedule(this.level);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
